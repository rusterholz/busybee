/.bundle/
!/.bundle/config
/.yardoc
/_yardoc/
/coverage/
/pkg/
*.gem
/spec/reports/
/tmp/
/log/

# rspec failure tracking
.rspec_status

# Appraisal resolves these per machine and per CI run
gemfiles/*.gemfile.lock

# Demo app — Rails runtime artifacts
spec/demo/db/*.sqlite3
spec/demo/db/*.sqlite3-*
spec/demo/log/
spec/demo/tmp/
spec/demo/storage/

# Docker volumes (data is stored in named volumes, not tracked in git)
.docker/
