# OMS domain workers — order lifecycle tracking.
#
# UpdateOrderStatusWorker is a single worker that handles all order status
# transitions, receiving the target status via a BPMN header.

workers:
  - Oms::LoadOrderAddressWorker
  - Oms::UpdateOrderStatusWorker
